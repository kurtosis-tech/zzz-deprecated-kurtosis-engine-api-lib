package kurtosis_engine_server_rpc_api_consts

const (
	// Protocol that the engine server container listens on
	ListenProtocol = "tcp"

	// The port that the engine server container listens on
	ListenPort = 9710
)
